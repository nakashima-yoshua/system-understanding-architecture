# Database Design

Document logical data ownership, schema intent, important constraints, lifecycle rules, migrations, and consumers.

The database model should be traceable to capabilities and business rules, not documented as an isolated technical artifact.
