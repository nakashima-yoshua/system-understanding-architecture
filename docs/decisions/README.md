# Architecture Decision Records

Use ADRs for decisions whose rationale would otherwise be lost and whose reversal would have meaningful cost.

Naming convention:

```text
ADR-0001-short-title.md
```

Each ADR should contain: status, context, decision, alternatives considered, consequences, and links to affected capabilities/design/code.

Use the template in `docs/templates/adr-template.md`.
